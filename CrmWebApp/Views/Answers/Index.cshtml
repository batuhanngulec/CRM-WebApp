@model IEnumerable<RmosCrmWebApp.Models.EntityClasses.Cevaplar>
@{
    SelectList dil = ViewBag.DilAdlar as SelectList;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Hover Rows -->
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            
            <div class="header">
                <h2 class="col-sm-11">
                    CEVAPLAR
                    <small>Cevaplar Example 1</small>
                </h2>
                <div class="col-sm-1">
                    <a href="@Url.Action("Create","Answers")" class="btn btn-success btn-circle  waves-effect waves-circle waves-float"><i class="material-icons">add</i></a>
                </div>
                <hr />
            </div>
            <div class="body">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Cevap Kodu</th>
                            <th>Cevap Set Sırası</th>
                            <th>Aktiflik</th>
                            <th>Güncelleme</th>
                            <th>Silme</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var con in Model)
                        {
                            <tr>
                                <td class="col-sm-3">@con.CevapKod</td>
                                <td class="col-sm-3">@con.SetSirasi</td>
                                <td class="col-sm-3">@(con.Aktiflik==true?"Aktif":"Pasif")</td>
                                <td class="col-sm-3"><a href="@Url.Action("Edit","Answers",new {id= con.CevapID })" class="btn btn-warning btn-circle waves-effect waves-circle waves-float"><i class="material-icons">edit</i></a></td>
                                <td class="col-sm-3"><a href="@Url.Action("Delete","Answers",new {id= con.CevapID })" onclick="return confirm('Seçtiğiniz bilgiler silinecek! Emin misiniz?')" class="btn btn-danger btn-circle waves-effect waves-circle waves-float"><i class="material-icons">delete</i></a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>



@*<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2 class="col-sm-2">
                    CEVAPLAR
                    <small>Cevaplar Example 1</small>
                </h2>
                <br />
            </div>
            <div class="body">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Cevaplar</th>
                            <th class="col-">@foreach (var item in (SelectList)ViewBag.DilAdlar)
                            {
                                @item.Text
                            }</th>
                        </tr>

                       

                    </thead>
                    <tbody >
                        @foreach (var con in Model)
                        {
                            <tr>
                                <td class="col-sm-3">@con.CevapKod</td>
                                <td class="col-sm-3">asd</td>
                                <td class="col-sm-3">asd</td>
                                <td class="col-sm-3">asd</td>
                                <td class="col-sm-3">asd</td>
                                <td class="col-sm-3">asd</td>
                                
                            </tr>

                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>*@
<!-- #END# Hover Rows -->
