@model RmosCrmWebApp.Models.EntityClasses.Cevaplar

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>
                    Yeni Cevaplar
                    <small>Güncelleme İşlemi</small>
                </h2>
            </div>
            <div class="body">
                <div class="row clearfix">
                    <div class="col-sm-12">
                        @using (Html.BeginForm())
                        {
                            @Html.AntiForgeryToken()

                            <div class="form-horizontal">
                                
                                
                                <label for="sorukod" class="control-label">Cevap Kodu:</label>
                                <div class="form-line">
                                    @Html.EditorFor(model => model.CevapKod, new { htmlAttributes = new { @class = "form-control", required = "required" } })
                                    @Html.ValidationMessageFor(model => model.CevapKod, "", new { @class = "text-danger" })
                                </div>


                                <label for="siraid" class="control-label">Anket Sırası:</label>
                                <div class="form-line">
                                    @Html.EditorFor(model => model.SetSirasi, new { htmlAttributes = new { @class = "form-control", required = "required", min = "1" } })
                                    @Html.ValidationMessageFor(model => model.SetSirasi, "", new { @class = "text-danger" })
                                </div>

                                                               
                                <div class="form-line">

                                    <div class="checkbox">
                                        <input type="checkbox" id="isActive" name="isActive" value="true" />
                                        <label for="isActive">Aktiflik</label>
                                    </div>
                                </div>

                                <div class="form-line">
                                    <div>
                                        <input type="submit" value="Oluştur" class="btn btn-primary" />
                                        @Html.ActionLink("Geri Dön", "Index")
                                    </div>
                                </div>
                            </div>
                        }

                        @if (ViewBag.result != null)
                        {
                            <h2><span class="label label-@(ViewBag.result[0]) col-sm-12">@(ViewBag.result[1])</span></h2>
                        }
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>