@model IEnumerable<RmosCrmWebApp.Models.EntityClasses.CevapSetleri>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Hover Rows -->
<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div id="alert_area"></div>
            <div class="header">
                <h2 class="col-sm-11">
                    CEVAP SETLERİ // Oluşturulan soruların iyi, kötü, orta olarak hangi soruların istenildiğini ayarlamak
                    <small>Cevap Setleri Example1</small>
                </h2>
                <div class="col-sm-1">
                    <a href="@Url.Action("Create","AnswerSets")" class="btn btn-success btn-circle  waves-effect waves-circle waves-float"><i class="material-icons">add</i></a>
                </div>
                <hr />
            </div>
            <div class="body">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Cevap Set Kodu</th>
                            <th>Cevap Set Adı</th>
                            <th>Aktiflik</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var con in Model)
                        {
                            <tr>
                                <td class="col-sm-6">@con.CevapSetKod</td>
                                <td class="col-sm-6">@con.Ad</td>
                                <td class="col-sm-6">@(con.Aktiflik==true?"Aktif":"Pasif")</td>
                                <td class="col-sm-6"><a href="@Url.Action("Edit","AnswerSets",new {id= con.CevapSetID })" class="btn btn-warning btn-circle waves-effect waves-circle waves-float"><i class="material-icons">edit</i></a></td>
                                <td class="col-sm-6"><a href="@Url.Action("Delete","AnswerSets",new {id= con.CevapSetID})" onclick="return confirm('Seçtiğiniz bilgiler silinecek! Emin misiniz?')" class="btn btn-danger btn-circle waves-effect waves-circle waves-float"><i class="material-icons">delete</i></a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- #END# Hover Rows -->
